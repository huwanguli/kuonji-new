# 说明：本栈不再使用环境变量注入配置。
# 所有连接信息（MySQL / Redis / JWT / 管理员）统一放在外挂的
#   ./config/config.yaml  （参考 config/config.example.yaml 模板）
# 部署时在服务器上创建：
#   /root/kuonji-deploy/
#     docker-compose.yml
#     config/config.yaml
#     uploads/
