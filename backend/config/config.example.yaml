server:
  port: 8080
  mode: debug

database:
  host: localhost
  port: 3306
  user: root
  password: your_password_here
  dbname: kuonji_new
  charset: utf8mb4
  max_idle_conns: 10
  max_open_conns: 100
  auto_migrate: false

redis:
  host: 127.0.0.1
  port: 6379
  password: ""
  db: 0

jwt:
  secret: change_this_to_a_random_string
  expire: 24h

admin:
  username: admin
  password: admin123
  email: admin@example.com

log:
  level: info
  format: json
  output: stdout
